# Disabled: no Google Scholar ID has been supplied. This is not a Pages deployment workflow.
name: Scholar disabled
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Scholar integration is disabled."
